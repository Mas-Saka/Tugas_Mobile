import 'package:flutter/material.dart';

class WaktuScreen extends StatelessWidget {
  const WaktuScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return const Scaffold(body: Center(child: Text('Waktu')));
  }
}
